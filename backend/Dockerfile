FROM n8nio/n8n:latest

# Switch to root to install additional tools if needed
USER root

# Install any extra OS packages here (e.g., for custom nodes)
# RUN apk add --no-cache <package>

# Switch back to the default n8n user
USER node
